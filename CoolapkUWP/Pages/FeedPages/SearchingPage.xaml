<feedpages:PivotPageBase
    x:Class="CoolapkUWP.Pages.FeedPages.SearchingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:feedpages="using:CoolapkUWP.Pages.FeedPages"
    xmlns:feedviewmodels="using:CoolapkUWP.ViewModels.FeedPages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:templates="using:CoolapkUWP.Controls.DataTemplates"
    xmlns:ui="using:CommunityToolkit.WinUI"
    d:DataContext="{d:DesignInstance Type=feedviewmodels:SearchingViewModel}"
    NavigationCacheMode="Required"
    mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <templates:FeedsTemplates />
                <templates:SearchWordTemplates />
                <templates:IndexCardTemplates />
                <templates:OtherDataTemplates />
            </ResourceDictionary.MergedDictionaries>
            <templates:CardTemplateSelector
                x:Key="CardTemplateSelector"
                ColorfulScrollCard="{StaticResource ColorfulScrollCardTemplate}"
                CapsuleListCard="{StaticResource CapsuleListCardTemplate}"
                Feed="{StaticResource FeedDefaultTemplate}"
                FeedListCard="{StaticResource FeedListCardTemplate}"
                FeedReply="{StaticResource FeedReplyDefaultTemplate}"
                GridScrollCard="{StaticResource GridScrollCardTemplate}"
                IconGridCard="{StaticResource IconGridCardTemplate}"
                IconLinks="{StaticResource IconLinksTemplate}"
                IconListCard="{StaticResource IconListCardTemplate}"
                IconLongTitleGridCard="{StaticResource IconLongTitleGridCardTemplate}"
                IconMiniGridCard="{StaticResource IconMiniGridCardTemplate}"
                ImageTextScrollCard="{StaticResource ImageTextScrollCardTemplate}"
                Images="{StaticResource ImageCardTemplate}"
                List="{StaticResource ListDefaultTemplate}"
                ListCard="{StaticResource ListCardTemplate}"
                LoginCard="{StaticResource LoginCardTemplate}"
                MessageCard="{StaticResource MessageCardTemplate}"
                Others="{StaticResource Empty}"
                ProductTimelineListCard="{StaticResource ProductTimelineListCardTemplate}"
                Rating="{StaticResource IconListFeedItemRatingTemplate}"
                RefreshCard="{StaticResource RefreshCardTemplate}"
                SortSelectCard="{StaticResource SortSelectCardTemplate}"
                SubtitleList="{StaticResource SubtitleListDefaultTemplate}"
                TitleCard="{StaticResource TitleCardTemplate}"
                User="{StaticResource UserTemplate}" />
        </ResourceDictionary>
    </Page.Resources>
    <Pivot
        x:Name="Pivot"
        Loaded="Pivot_Loaded"
        SelectionChanged="Pivot_SelectionChanged">
        <Pivot.RightHeader>
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                <StackPanel
                    x:Name="RightHeader"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <ComboBox
                        Margin="8,6,4,6"
                        VerticalAlignment="Center"
                        SelectedIndex="{x:Bind Provider.SearchFeedItemSource.SearchFeedTypeComboBoxSelectedIndex, Mode=TwoWay}">
                        <x:String>全部</x:String>
                        <x:String>动态</x:String>
                        <x:String>图文</x:String>
                        <x:String>点评</x:String>
                        <x:String>酷图</x:String>
                        <x:String>提问</x:String>
                        <x:String>回答</x:String>
                        <x:String>视频</x:String>
                        <x:String>二手</x:String>
                        <x:String>投票</x:String>
                    </ComboBox>
                    <ComboBox
                        Margin="4,6,8,6"
                        VerticalAlignment="Center"
                        SelectedIndex="{x:Bind Provider.SearchFeedItemSource.SearchFeedSortTypeComboBoxSelectedIndex, Mode=TwoWay}">
                        <x:String>实时</x:String>
                        <x:String>热度</x:String>
                        <x:String>评论数</x:String>
                    </ComboBox>
                </StackPanel>
                <Button
                    x:Name="RefreshButton"
                    x:Uid="/MainPage/RefreshButton"
                    Grid.Column="2"
                    Margin="-8,0,0,0"
                    VerticalAlignment="Center"
                    Click="RefreshButton_Click"
                    Content="&#xE72C;"
                    Style="{StaticResource NavigationIconButtonNormalStyle}">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="F5" />
                    </Button.KeyboardAccelerators>
                </Button>
            </StackPanel>
        </Pivot.RightHeader>
        <PivotItem Header="动态">
            <muxc:RefreshContainer RefreshRequested="RefreshContainer_RefreshRequested">
                <ListView
                    ui:ListViewExtensions.ItemContainerStretchDirection="Horizontal"
                    ItemContainerStyle="{StaticResource DefaultCardItemStyle}"
                    ItemTemplateSelector="{StaticResource CardTemplateSelector}"
                    ItemsSource="{x:Bind Provider.SearchFeedItemSource, Mode=OneWay}"
                    ShowsScrollingPlaceholders="False"
                    Style="{StaticResource DefaultCardListViewStyle}" />
            </muxc:RefreshContainer>
        </PivotItem>
        <PivotItem Header="用户">
            <muxc:RefreshContainer RefreshRequested="RefreshContainer_RefreshRequested">
                <ListView
                    ui:ListViewExtensions.ItemContainerStretchDirection="Horizontal"
                    ItemContainerStyle="{StaticResource DefaultCardItemStyle}"
                    ItemTemplateSelector="{StaticResource CardTemplateSelector}"
                    ItemsSource="{x:Bind Provider.SearchFeedItemSource, Mode=OneWay}"
                    ShowsScrollingPlaceholders="False"
                    Style="{StaticResource DefaultCardListViewStyle}" />
            </muxc:RefreshContainer>
        </PivotItem>
        <PivotItem Header="话题">
            <muxc:RefreshContainer RefreshRequested="RefreshContainer_RefreshRequested">
                <ListView
                    ui:ListViewExtensions.ItemContainerStretchDirection="Horizontal"
                    ItemContainerStyle="{StaticResource DefaultCardItemStyle}"
                    ItemTemplateSelector="{StaticResource CardTemplateSelector}"
                    ItemsSource="{x:Bind Provider.SearchFeedItemSource, Mode=OneWay}"
                    ShowsScrollingPlaceholders="False"
                    Style="{StaticResource DefaultCardListViewStyle}" />
            </muxc:RefreshContainer>
        </PivotItem>
    </Pivot>
</feedpages:PivotPageBase>
